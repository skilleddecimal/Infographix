# NOTE: This is a template. In production, use external secrets manager
# (AWS Secrets Manager, HashiCorp Vault, etc.) or sealed-secrets

apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  labels:
    app.kubernetes.io/name: infographix
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:password@postgres:5432/infographix"
  REDIS_URL: "redis://:password@redis:6379/0"
  SECRET_KEY: "change-this-in-production"
  STRIPE_SECRET_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""
  ANTHROPIC_API_KEY: ""
  SENTRY_DSN: ""
